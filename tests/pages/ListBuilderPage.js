// Locators + actions for the list builder (create/edit list). No assertions.
class ListBuilderPage {
  constructor(page) {
    this.page = page;
  }

  // locators

  // actions
}

module.exports = { ListBuilderPage };
