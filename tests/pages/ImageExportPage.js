// Locators + actions for the image export feature. No assertions.
class ImageExportPage {
  constructor(page) {
    this.page = page;
  }

  // locators

  // actions
}

module.exports = { ImageExportPage };
