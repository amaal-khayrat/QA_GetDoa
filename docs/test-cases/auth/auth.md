# Authentication — Sub-Features

Feature ID prefix: `AUTH`

## Sub-Features

- Google OAuth sign-in
- Email/password sign-up
- Session management (persist across page reloads)
- Route guard — unauthenticated redirect on `/dashboard/*` and `/admin`
- Logout
