# REST API — Sub-Features

Feature ID prefix: `API`

## Sub-Features

- `GET /api/doa` — paginated dua list with search
- `GET /api/doa/random` — random duas with optional count + category
- `GET /api/list/:listId` — public list data (auth/visibility enforced)
